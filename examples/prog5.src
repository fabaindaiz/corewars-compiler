(let (x 2)
  (let (rx x)
    (let (y 3)
      (let (ry y)
        (seq
          (JMP start)
          (SUB (store y) 0)
          (SUB 0 (store x))
          (SUB (store rx) (store ry))
          (label start)
          (ADD 4 (Ind rx))
          (ADD 5 (Ind ry)) )))))