(let (x A 1)
  (let (y B 2)
    (let (a A 0)
      (let (b B 0)
        (seq
          (JMP (Dir 2))
          (DAT (place x) (place y))
          (ADD x y)
          (ADD y y)
          (ADD y x)
          (MOV x a)
          (MOV a b)
          (DAT (place a) 0)
          (DAT 0 (place b)) )))))