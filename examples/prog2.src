(let (x 1)
  (let (y 2)
    (let (a 0)
      (let (b 0)
        (seq
          (JMP (Dir 2))
          (DAT (store x) (store y))
          (ADD x y)
          (ADD y y)
          (ADD y x)
          (MOV x a)
          (MOV a b)
          (DAT (store a) 0)
          (DAT 0 (store b)) )))))