(let (a A dest1)
  (let (b B dest2)
    (let (c B dest3)
      (let (d B dest4)
        (seq
          (JMP (Ind d))
          (label dest2)
          (JMP (Ind a) (place c))
          (label dest3)
          (JMP (Ind b) (place d))
          (DAT (place a) (place b))
          (label dest4)
          (JMP (Ind c))
          (label dest1)
          (MOV (Dir 0) (Dir 1)) )))))