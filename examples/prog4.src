(let (a dest1)
  (let (b dest2)
    (let (c dest3)
      (let (d dest4)
        (seq
          (JMP (Ind d))
          (label dest2)
          (JMP (Ind a) (store c))
          (label dest3)
          (JMP (Ind b) (store d))
          (DAT (store a) (store b))
          (label dest4)
          (JMP (Ind c))
          (label dest1)
          (MOV (Dir 0) (Dir 1)) )))))